{
  "name": "doors",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "rm -rf .next docs && next build && next export -o docs && touch docs/.nojekyll"
  },
  "dependencies": {
    "next": "9.0.7",
    "react": "16.10.2",
    "react-dom": "16.10.2"
  }
}